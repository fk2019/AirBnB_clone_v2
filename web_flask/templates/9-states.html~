<!DOCTYPE html>
<HTML lang="en">
  <HEAD>
    <TITLE>HBNB</TITLE>
  </HEAD>
  <BODY>
    <H1> States </H1>
    {% if states is mapping %}
    <UL>
      {% for item in states %}
      {% for key, value in item.items() %}
      <LI>{{ key|e }}: <B>{{ value|e }}</B></LI>
      {% endfor %}
      {% endfor %}
    </UL>

    <H1>State: {{ list(states.keys())[0] }} </H1>
    <H3>Cities:</H3>
    <UL>
      {% for value in states.values() %}
      <LI>{{ value['id']|e }}: <B>{{ value['name']|e }}</B></LI>
      {% endfor %}
    </UL>
  </BODY>
</HTML>
